version: "3.9"
services:
  # backend:
    # container_name: backend
    # build:
    #   context: ./docker
    #   dockerfile: ./backend.Dockerfile
    # ports:
    #   - "3000:3000"
    # volumes:
    #   - ./backend:/app
    # networks:
    #   - backend
    # depends_on:
    #   - db
  postgres:
    image: postgres:latest
    env_file:
      - ./docker/postgres.env
    restart: unless-stopped
    ports:
     - 5432:5432
  pgadmin:
    image: dpage/pgadmin4:latest
    restart: unless-stopped
    env_file:
      - ./docker/pgadmin.env
    volumes:
      - ./docker/pgadmin_servers.json:/pgadmin4/servers.json
    ports:
      - 5050:80
    depends_on:
      - postgres
  # web:
  #   image: node:lts
  #   build: .
  #   # working_dir: $(SG_PATH):/app/

# volumes:
#   db_data:
#     name: db_data
#     driver_opts:
#       type: none
#       device: ${SG_PATH}
#       o: bind


# networks:
#   backend:
